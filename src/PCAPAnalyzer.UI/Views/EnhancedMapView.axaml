<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:PCAPAnalyzer.UI.Controls"
             xmlns:vm="using:PCAPAnalyzer.UI.ViewModels"
             mc:Ignorable="d" d:DesignWidth="1400" d:DesignHeight="900"
             x:Class="PCAPAnalyzer.UI.Views.EnhancedMapView"
             x:DataType="vm:EnhancedMapViewModel">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="#0D1117" Margin="24,24,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Map Control with Tab Navigation -->
            <Border Grid.Row="0" Classes="modern-card" Margin="0,0,0,24">
                <controls:ContinentMapControlV2
                    x:Name="MapControl"
                    CountryData="{Binding CountryTrafficData}"
                    ShowAnimations="{Binding AnimateFlows}"
                    Margin="20,20,20,20"
                    Height="500"/>
            </Border>
            
            <!-- Country Tables Section -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Countries by Packets Table -->
                <Border Grid.Column="0" Classes="modern-card" Margin="0,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Table Header -->
                        <Border Grid.Row="0" Classes="table-header">
                            <Grid Margin="20,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="3" Height="20" Background="#3B82F6" CornerRadius="1.5" Margin="0,0,12,0"/>
                                    <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#F0F6FC">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}Top {0} Countries by Packets">
                                                <Binding Path="CountryDisplayCount"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                                
                                <Button Grid.Column="1" 
                                        Command="{Binding ToggleCountryDisplayCommand}"
                                        Background="#1C2128"
                                        BorderBrush="#30363D"
                                        Padding="8,4">
                                    <TextBlock Text="{Binding ShowTop100, Converter={StaticResource CountryTableToggleConverter}}" 
                                               FontSize="11" 
                                               Foreground="#8B949E"/>
                                </Button>
                            </Grid>
                        </Border>
                        
                        <!-- Table Content -->
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding TopCountriesByPackets}" Margin="0,10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="20,0" Background="#161B2220" CornerRadius="4" Padding="4,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="40"/>
                                                    <ColumnDefinition Width="50"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="80"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Rank -->
                                                <TextBlock Grid.Column="0" Text="{Binding Rank}"
                                                          FontSize="12" FontWeight="Bold"
                                                          Foreground="#7D8590" VerticalAlignment="Center"/>
                                                
                                                <!-- Country Code -->
                                                <Border Grid.Column="1" Background="#21262D" CornerRadius="4" 
                                                       Padding="6,3" HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding CountryCode}" 
                                                              FontSize="11" FontWeight="Bold" Foreground="#F0F6FC"/>
                                                </Border>
                                                
                                                <!-- Country Name and Continent -->
                                                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                                                    <TextBlock Text="{Binding CountryName}" 
                                                              FontSize="12" Foreground="#E6EDF3"/>
                                                    <TextBlock Text="{Binding Continent}" 
                                                              FontSize="10" Foreground="#8B949E"/>
                                                </StackPanel>
                                                
                                                <!-- Percentage Bar -->
                                                <Grid Grid.Column="3" Margin="10,0">
                                                    <Border Background="#21262D" CornerRadius="3" Height="20">
                                                        <Grid ClipToBounds="True">
                                                            <Border Background="{Binding PacketPercentage, Converter={StaticResource PercentageToGradientConverter}}"
                                                                   CornerRadius="3"
                                                                   HorizontalAlignment="Left"
                                                                   Width="{Binding PacketPercentage, Converter={StaticResource PercentageToWidthConverter}}"/>
                                                            <TextBlock Text="{Binding PacketPercentage, StringFormat='{}{0:F1}%'}"
                                                                      FontSize="10"
                                                                      FontWeight="SemiBold"
                                                                      Foreground="White"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                                
                                                <!-- Packets -->
                                                <TextBlock Grid.Column="4" FontSize="12" FontWeight="SemiBold" 
                                                          Foreground="#58A6FF" VerticalAlignment="Center"
                                                          HorizontalAlignment="Right">
                                                    <TextBlock.Text>
                                                        <MultiBinding StringFormat="{}{0:N0}">
                                                            <Binding Path="PacketCount"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                                
                                                <!-- Detail Button -->
                                                <Button Grid.Column="5" 
                                                        Command="{Binding $parent[UserControl].DataContext.ShowCountryDetailsCommand}"
                                                        CommandParameter="{Binding}"
                                                        Background="#1C2128"
                                                        BorderBrush="#30363D"
                                                        Padding="6,3"
                                                        Margin="5,0,0,0"
                                                        HorizontalAlignment="Center">
                                                    <TextBlock Text="Details" FontSize="11" Foreground="#8B949E"/>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                    </Grid>
                </Border>
                
                <!-- Countries by Bytes Table -->
                <Border Grid.Column="1" Classes="modern-card" Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Table Header -->
                        <Border Grid.Row="0" Classes="table-header">
                            <Grid Margin="20,0">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="3" Height="20" Background="#10B981" CornerRadius="1.5" Margin="0,0,12,0"/>
                                    <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#F0F6FC">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}Top {0} Countries by Bytes">
                                                <Binding Path="CountryDisplayCount"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- Table Content -->
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding TopCountriesByBytes}" Margin="0,10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="20,0" Background="#161B2220" CornerRadius="4" Padding="4,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="40"/>
                                                    <ColumnDefinition Width="50"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="80"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Rank -->
                                                <TextBlock Grid.Column="0" Text="{Binding Rank}"
                                                          FontSize="12" FontWeight="Bold"
                                                          Foreground="#7D8590" VerticalAlignment="Center"/>
                                                
                                                <!-- Country Code -->
                                                <Border Grid.Column="1" Background="#21262D" CornerRadius="4" 
                                                       Padding="6,3" HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding CountryCode}" 
                                                              FontSize="11" FontWeight="Bold" Foreground="#F0F6FC"/>
                                                </Border>
                                                
                                                <!-- Country Name and Continent -->
                                                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                                                    <TextBlock Text="{Binding CountryName}" 
                                                              FontSize="12" Foreground="#E6EDF3"/>
                                                    <TextBlock Text="{Binding Continent}" 
                                                              FontSize="10" Foreground="#8B949E"/>
                                                </StackPanel>
                                                
                                                <!-- Percentage Bar -->
                                                <Grid Grid.Column="3" Margin="10,0">
                                                    <Border Background="#21262D" CornerRadius="3" Height="20">
                                                        <Grid ClipToBounds="True">
                                                            <Border Background="{Binding BytePercentage, Converter={StaticResource PercentageToGradientConverter}}"
                                                                   CornerRadius="3"
                                                                   HorizontalAlignment="Left"
                                                                   Width="{Binding BytePercentage, Converter={StaticResource PercentageToWidthConverter}}"/>
                                                            <TextBlock Text="{Binding BytePercentage, StringFormat='{}{0:F1}%'}"
                                                                      FontSize="10"
                                                                      FontWeight="SemiBold"
                                                                      Foreground="White"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </Grid>
                                                
                                                <!-- Bytes -->
                                                <TextBlock Grid.Column="4" FontSize="12" FontWeight="SemiBold" 
                                                          Foreground="#10B981" VerticalAlignment="Center"
                                                          HorizontalAlignment="Right">
                                                    <TextBlock.Text>
                                                        <Binding Path="ByteCount" Converter="{StaticResource ByteSizeConverter}"/>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                                
                                                <!-- Detail Button -->
                                                <Button Grid.Column="5" 
                                                        Command="{Binding $parent[UserControl].DataContext.ShowCountryDetailsCommand}"
                                                        CommandParameter="{Binding}"
                                                        Background="#1C2128"
                                                        BorderBrush="#30363D"
                                                        Padding="6,3"
                                                        Margin="5,0,0,0"
                                                        HorizontalAlignment="Center">
                                                    <TextBlock Text="Details" FontSize="11" Foreground="#8B949E"/>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>